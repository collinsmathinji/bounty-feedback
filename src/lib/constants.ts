/** Shown while OCR is running; replaced by backend or by UI with OCR_FALLBACK_MESSAGE. */
export const FEEDBACK_PROCESSING_PLACEHOLDER = '(Processing attachments…)';

/** Shown when no text was extracted (e.g. OCR timed out). */
export const OCR_FALLBACK_MESSAGE =
  'No text was extracted. OCR took longer than expected.';
